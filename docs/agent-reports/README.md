# Agent reports

Each Codex agent writes `agent-<letter>.md` here when it finishes: what it built, files, natives/memory added, config added, what is unverified in game, and requests for the orchestrator.
